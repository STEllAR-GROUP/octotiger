#!groovy

pipeline {
    agent any

    stages {
        stage('checkout') {
            steps {
                echo "Git commit ${env.GIT_COMMIT} or ${GIT_COMMIT} ?"
                echo "Running ${env.BUILD_ID} on ${env.JENKINS_URL}"
            }
        }
    }
}
